"""Admin placeholder for the authentication app.

This app currently does not define custom models. The file exists
so contributors can add admin registrations here in the future
without creating merge conflicts.
"""

from django.contrib import admin

# No models to register at the moment.
